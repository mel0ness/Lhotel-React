import {HelmetProvider} from "react-helmet-async"

const Reservation = () => {

    return (<div>

        <HelmetProvider>
            <title>Réservation</title>
        </HelmetProvider>
    </div>)
}

export default Reservation;